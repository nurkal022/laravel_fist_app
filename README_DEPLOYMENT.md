# Развертывание журнала посещаемости на Plesk (Laravel 11 + SQLite)

## ✅ **Проект готов к развертыванию!**

Проект был пересоздан с **Laravel 11** для совместимости с **PHP 8.3.28**.

## 📦 **Что создано:**
- `attendance_journal_laravel11.zip` - готовый архив для загрузки
- Laravel 11.46.2 (совместим с PHP 8.2+)
- SQLite база данных с созданными таблицами
- Все модели, контроллеры и представления перенесены

## 🚀 **Инструкция по развертыванию на Plesk:**

### Шаг 1: **Подготовка Git репозитория**
```bash
# Создайте новый репозиторий на GitHub/GitLab
# Загрузите архив attendance_journal_laravel11.zip
# Распакуйте и загрузите файлы в репозиторий
git add .
git commit -m "Laravel 11 attendance journal"
git push origin main
```

### Шаг 2: **Настройка домена в Plesk**
1. **Domains** → выберите домен
2. **PHP Settings** → установите **PHP 8.2** или **8.3**
3. **Document root** → установите `public`

### Шаг 3: **Git интеграция**
1. Перейдите в **Git** для домена
2. **Add Repository**:
   - **Repository URL**: `https://github.com/YOUR_USERNAME/attendance-journal.git`
   - **Branch**: `main`
3. **Enable additional deployment actions** и добавьте:

```bash
# Установка зависимостей
composer install --optimize-autoloader --no-dev

# Настройка окружения
cp .env.example .env

# Генерация ключа
php artisan key:generate

# Создание базы данных
touch database/database.sqlite
chmod 664 database/database.sqlite

# Запуск миграций
php artisan migrate --force

# Кэширование
php artisan config:cache
php artisan route:cache
php artisan view:cache

# Storage link
php artisan storage:link

# Права доступа
chmod -R 755 storage/
chmod -R 755 bootstrap/cache/
```

### Шаг 4: **Настройка .env файла**
После первого деплоя создайте `.env` файл через **Files** в Plesk:

```env
APP_NAME="Журнал посещаемости"
APP_ENV=production
APP_KEY=ВАШ_КЛЮЧ_ИЗ_ЛОГОВ
APP_DEBUG=false
APP_URL=https://ваш-домен.com

DB_CONNECTION=sqlite
DB_DATABASE=/var/www/vhosts/ваш-домен.com/httpdocs/database/database.sqlite

LOG_CHANNEL=stack
CACHE_STORE=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
```

### Шаг 5: **Проверка развертывания**
1. Откройте сайт в браузере
2. Должны работать все функции:
   - Список студентов
   - Добавление студентов
   - Отметка посещаемости
   - Отчеты

## 🔧 **Устранение проблем:**

### **Если сайт не загружается:**
```bash
# Проверьте логи в Plesk Files
tail /var/www/vhosts/domain.com/logs/error_log
```

### **Если проблемы с базой данных:**
```bash
# В SSH Plesk проверьте
ls -la database/
chmod 664 database/database.sqlite
```

### **Перезапуск деплоя:**
- В Plesk Git → **Pull & Deploy**

## ✅ **Совместимость:**
- ✅ PHP 8.3.28
- ✅ Laravel 11.46.2
- ✅ SQLite база данных
- ✅ Все зависимости совместимы

## 📁 **Структура после развертывания:**
```
/var/www/vhosts/domain.com/
├── httpdocs/           # Корень сайта (artisan здесь)
│   ├── app/
│   ├── config/
│   ├── database/
│   │   └── database.sqlite
│   ├── public/         # Корневая папка сайта
│   ├── resources/
│   ├── routes/
│   ├── storage/
│   ├── artisan
│   └── .env
```

**Проект готов к развертыванию! 🚀**
